/*

    La carpeta public:

        Almacena principalmente el archivo index.html 
            -Va a ser el que se cargue cuando el usuario entre a la URL. 
            -Dentro de este archivo se encuentra un div con id=”root”, donde se va a insertar nuestra aplicación de React.

    La carpeta src:

        Todo el trabajo en React se hace principalmente dentro de esta carpeta. 
            -Dentro del archivo index.js es que renderizamos el div con id=”root”.
            -App.js se exporta al archivo index.js que renderizara todo este componente en el index.html
        
*/