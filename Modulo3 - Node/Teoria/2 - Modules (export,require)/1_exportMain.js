const exprt = require('./1_exportaciones.js'); //si 1_exportaciones.js se llama index.js en otra carpeta solo puedo hacer referencia a esa carpeta y no hace falta poner el nombre del archivo

console.log(exprt);
