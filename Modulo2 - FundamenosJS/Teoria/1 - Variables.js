var nombre1; //variable global
let nombre2; //la variable solo existe en su bloque de codigo
const nombre3 = 3; //constante (igual que let y no se podra cambiar el valor asignado)

var a = 1, b = 2, c = a + b;    // variables en una sola linea
